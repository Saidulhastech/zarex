---
const { class: className = '' } = Astro.props;
---

<span class={`inline-flex items-center rounded-full border border-slate-200 bg-white px-3 py-1 text-xs font-semibold uppercase tracking-wide text-slate-600 ${className}`.trim()}>
  <slot />
</span>
